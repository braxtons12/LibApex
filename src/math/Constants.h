#pragma once

namespace apex {
	namespace math {
		const float pif = 3.1415926535897932384626433f;
		const float twoPif = 2.0f * pif;
		const float twoOverPif = 2.0f / pif;
		const float piOver2f = pif / 2.0f;
		const float threePiOver2f = 3.0f * piOver2f;
		const float fourOverPif = 4.0f / pif;
		const float piOver4f = pif / 4.0f;
		const float piOver6f = pif / 6.0f;
		const float piOver12f = pif / 12.0f;

		const double pi = 3.1415926535897932384626433;
		const double twoPi = 2.0 * pi;
		const double twoOverPi = 2.0 / pi;
		const double piOver2 = pi / 2.0;
		const double threePiOver2 = 3.0 * piOver2;
		const double fourOverPi = 4.0 / pi;
		const double piOver4 = pi / 4.0;
		const double piOver6 = pi / 6.0;
		const double piOver12 = pi / 12.0;
	}
}
